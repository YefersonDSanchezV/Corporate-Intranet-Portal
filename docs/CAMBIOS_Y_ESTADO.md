# Intranet Institucional — Registro de Cambios y Estado del Sistema

**Fecha:** 22/09/2026  
**Repo:** `C:\Users\PRACTICANTE.SISTEMAS\Documents\Proyectos\Intranet Institucional`  
**Stack:** Frontend Vite 6 + React 18.3.1 + Tailwind 4 | Backend Spring Boot 3.5.16 Java 21 + PostgreSQL 17.5 + Flyway | Docker Compose (nginx + Spring)

---

## 1. Resumen Ejecutivo

Sistema 100% funcional frontend ↔ backend conectado via `/api` (proxy nginx en prod `backend:8080`, `vite.config.ts:34` proxy a `localhost:8080` en dev). Migraciones Flyway `V1..V11` aplicadas, backend `Healthy` (`/actuator/health`), frontend `http://localhost:3000` OK. Todas las incidencias de `docs/Mejoras y cambio/Mejoras y cambios.docx` aplicadas y verificadas con `npm run build`, `mvn test`, `docker compose build`.

---

## 2. Integración Frontend-Backend y Correcciones Base

### 2.1 Frontend Build Docker
- **Problema:** `package.json:10` `react`/`react-dom` solo en `peerDependencies` → `docker build` falla `Cannot find react`.
- **Fix:** `Corporate-Intranet-Portal/package.json:10` añadidos `react@18.3.1`, `react-dom@18.3.1` a `dependencies`.
- **Fix:** `Dockerfile:12` `COPY package-lock.json*` + `npm install --legacy-peer-deps`, `ARG VITE_API_BASE_URL` (`/api`).

### 2.2 CORS y Seguridad
- **Fix:** `intranet-backend/src/main/java/co/com/icvc/intranet_backend/config/CorsConfig.java:13` `allowedOriginPatterns("*")`, `maxAge 3600`, `allowCredentials false`.
- **Fix:** `security/SecurityConfig.java:40` reorden: `POST/PUT/PATCH/DELETE /api/sites|directory → authenticated` antes de `hasRole("ADMIN")`, luego `GET /** permitAll`.

### 2.3 Sitios `active` toggle
- `portal/service/SiteService.java:58` añadido `toggleActive(id)` y `controller/SiteController.java:52` ahora llama `toggleActive` (antes `get`).

### 2.4 Nginx
- `Corporate-Intranet-Portal/nginx.conf:26` `proxy /api/` con timeouts `10s/30s` + bloque `/actuator/` para health.

### 2.5 Docker Compose
- Nuevo `docker-compose.yml` unificado (backend `env_file .env`, frontend `args VITE_API_BASE_URL`, volúmenes `backend-uploads/logs`, `depends_on: service_healthy`).
- `docker-compose.dev.yml` y `docker-compose.prod.yml` añadidos `healthcheck` frontend, volúmenes, `SPRING_PROFILES_ACTIVE`.

### 2.6 Base de Datos — Migraciones
- **V8 `seed_base_catalogs.sql:5`** `INSERT` con `oid` explícito `COALESCE(MAX(oid)+1)` compatible con BD existente sin `IDENTITY` (fix `null oid` en `genmodulo`).
- **V9 `fix_identity_columns.sql:1`** `ALTER TABLE ... ADD GENERATED BY DEFAULT AS IDENTITY` idempotente para 27 tablas + `setval(GREATEST(1, MAX(oid)))`.
- **V10 `clear_default_sites_and_extensions.sql:5`** `DELETE FROM gensitredireccion/gendircextenciones/gendircorreo/innovanalitica...` + `setval(...,1,false)` para creación manual desde cero.
- **V11 `seed_pisos_areas.sql:8`** inserta `Piso 1-6`, `Betania` y áreas `Sistemas, Comunicaciones, Cartera, Facturación, Talento Humano, Administrativa, Asistencial`.

**Verificación:** `mvnw clean package -DskipTests → BUILD SUCCESS`, `mvnw test → 15 tests 0 failures`, `vite build 2549 modules 517kB`, `docker compose build`, `curl /actuator/health → UP`, `POST /api/auth/login admin/Admin2026** → 200 {token,roles:[ADMIN]}`.

---

## 3. Mejoras y Cambio — Docx Históricos Aplicados

### 3.1 Docx 22/09 08:23 — 4 ítems (Interfaz inicial / Asistencial / Gestión / Soporte)
1. **Interfaz inicial sin sitios por defecto:** `QuickAccessSection.tsx:14` eliminado `dynamicSites` (`moduleId Inicio`), solo `Extensiones Telefónicas`. BD `V10` borra sitios + `SystemContext.tsx:176` `localStorage intranet_clean_v10` limpia `intranet_sites/eps/directory`.
2. **Área Asistencial solo Consulta Externa:** `ClinicalAreaModule.tsx:12` `baseApps` → solo `Consulta Externa`; `ClinicalAreaPanel.tsx:8` idem + `ExternalConsultationModal`.
3. **Gestión Institucional sin Almera:** `InstitutionalManagementModule.tsx:6` y `InstitutionalManagementPanel.tsx:6` removido `Almera - Sistema de Gestión de Calidad`.
4. **Soporte sin GLPI:** `SupportModule.tsx:6` removido `GLPI - Mesa de Ayuda`.

### 3.2 Docx 22/09 09:xx — 2 errores (Extensión larga / Sitios no visualizados)
1. **Extensión larga deforma tabla + redimensionar + filtros módulo:** `GeneralesDirectorioView.tsx:202` `DirectoryTable`/`EmailTable` `table-fixed` + `colgroup` widths + `truncate break-all font-mono` + `Th` `cursor-col-resize` + `FILTER por módulo` y `GeneralesSitiosView.tsx:90` `table-fixed` + `url truncate break-all` + filtros `Todos + MODULE_OPTIONS` con `truncate max-w`.
2. **Sitios no visualizados en panel público:** `QuickAccessSection.tsx:14` restaurado `dynamicSites` (`Inicio`), `ClinicalAreaModule.tsx:24` `filter (Area Asistencial||Clinical)`, `InstitutionalManagementModule.tsx:34` `(Gestion Institucional||Institutional)`, `SupportModule.tsx:34` `(Soporte||Support)`, `AdministrativeAreaModule.tsx:28` `(Area Administrativa||Administrative)` — fix `moduleId` mismatch con `mappers.ts:22` `moduloNombre`.

### 3.3 Docx 22/09 con imagen RedirectModal (quitale esto)
**Eliminar modal `Redirigiendo...` 3s:** `modals/RedirectModal.tsx:12` `countdown + window.open` eliminado de 9 vistas (`QuickAccessSection`, `ClinicalAreaModule/Panel`, `InstitutionalManagementModule/Panel`, `SupportModule/Panel`, `AdministrativeAreaModule/Panel`) → ahora `if (url) window.open(url,"_blank")` directo. Build `2549 modules 522.92kB`.

### 3.4 Área Administrativa Biometric
**Eliminar Biometric estándar:** `AdministrativeAreaModule.tsx:17` y `AdministrativeAreaPanel.tsx:14` eliminado `baseApps=[Biometric]`, ahora `adminApps = sites.filter(Area Administrativa)` solo dinámicos. Verificado `GET /api/sites` BUK x2 ahora únicos.

### 3.5 Docx 4 ítems (Usuario modal / Botón Grabar / Crear Usuario / Directorio)
1. **Modal flotante Crear Usuario:** `GeneralesUsuariosView.tsx:14` `showCreateModal` + `fixed inset-0 bg-black/40 backdrop-blur-sm` header `bg-[#0778AC]` con form 2 columnas (mismo que página vieja), `handleCreateUser` cierra modal, `handleAuthorize` abre modal.
2. **Botón Grabar:** línea 272 `Guardar Usuario` → `Grabar` (ambos inline y modal).
3. **Eliminar módulo Crear Usuario:** `AdminSidebar.tsx:51` removido `children crear-usuario` de `MENU`, `AdminPanel.tsx:59` queda huérfano.
4. **Sitio Directorio inhabilitado:** `GeneralesSitiosView.tsx:12` `DISABLED_MODULES={"Directorio"}`, `select` options disabled, `handleRegister` guard, filtros `opacity-60 inhabilitado`.

### 3.6 Docx modal Sitio como Usuario
**Sitio modal flotante igual usuario:** `GeneralesSitiosView.tsx:111` `showForm` ya no togglea página, ahora `fixed backdrop-blur` modal `max-w-3xl` con header, `Grabar` footer, tabla siempre visible.

### 3.7 Docx 8 ítems (último, 9 imágenes)
1. **Botón sitio → Grabar:** `GeneralesSitiosView.tsx:222` `Crear sitio/Guardar cambios` → `Grabar`.
2. **Directorio Extensiones Rol vs Área + Piso Betania:** `GeneralesDirectorioView.tsx:5` `PISOS=["Piso 1".."Piso 6","Betania"]`, `rol` state `asistencial/administrativo` dropdown + `Area` informativo, `piso` `select` Betania, `saveExtension` `type: rol, floor:[piso]`; `DirectoryModule.tsx:8` `FLOORS` igual; `V11` seed Betania. `SystemContext.tsx:390` `merged type/area` preserva `rol` tras fetch (mapper hardcodea `administrativo`).
3. **Es soporte → Soporte:** `DirectoryModule.tsx:61` `asistencial/administrativo` ahora `&& !isSupport`; `ITSupportContactsModal.tsx:14` merge `directory.filter(isSupport)` y `institutionEmails.filter(isSupport)` en `extensionContacts/emailContacts`.
4. **Correo público Cargar + acciones:** `DirectoryModule.tsx:271` `Cargar Nuevo Correo` ahora `{canManage && <button>}`, `Correos` tabla `Eye` siempre, `Pencil` solo `canManage`.
5. **Correo área/cargo + ubicación→cargo + filtros área/cargo:** `GeneralesDirectorioView` admin ya filtra, `DirectoryModule.tsx:290` añadidos `emailAreaFilter/emailCargoFilter` selects + `filter(e=> ... && areaFilter && cargoFilter)`, modal `showViewEmailModal` `Ubicación/Piso -> Cargo` (`Briefcase`, `{position}`).
6. **Estilo modals extensión/correo igual usuario:** `GeneralesDirectorioView.tsx:236` `consulting` modal `backdrop-blur` con `DetailRow` (Extensión: 6 campos Nombre/Número/Piso/Área/Tipo/Estado; Correo: 4 campos Nombre/Cargo/Email/Área sin floor).
7. **Phone se elimina:** `AuthContext.tsx:222` `addUser/updateUser` `merged = {...created, phone: userData.phone}` preserva teléfono localmente (backend `genusuario` sin columna teléfono).
8. **Correo Es soporte:** `SystemContext.tsx:100` `InstitutionEmail` añade `isSupport?`, `api/mappers.ts:43` `mapCorreoToFE` mapea `soporte`, `api/directory.ts:69` `soporte: !!email.isSupport`, `GeneralesDirectorioView.tsx:23` `emailIsSupport` + checkbox `Es soporte` en modal correo, `SystemContext.tsx:501` `merged isSupport`.

---

## 4. Estado Actual del Sistema

### 4.1 Frontend `Corporate-Intranet-Portal`
- **Home:** `QuickAccessSection` solo Extensiones + sitios `Inicio` dinámicos (si existen) con `truncate`, `window.open` directo.
- **Directorios:** `DirectoryModule` `FLOORS Todos/Piso1-6/Betania`, `Extensiones` grid Asistencial/Administrativo `!isSupport` con `truncate break-all` + chip `Área` + `floor`, filtros `Piso` píldoras y `Área/Cargo` selects fuera de tabla; `Cargar Nuevo Directorio` ahora `Nombre`+`Área`+`Extensión`+`Piso(s)` independientes; `Correos` tabla `Cargo`/`Área` independientes (`V13`), `Cargar` solo `canManage`, consulta modal `Cargo` no `Piso`.
- **Admin:** `GeneralesUsuariosView` lista con filtros `status→select`, `cargo→select`, modal flotante `Crear Usuario` blur `Grabar`; `GeneralesDirectorioView` tablas `table-fixed` resizable, filtros `Filtros: Piso/Área` y `Área/Cargo` fuera de `<Th>` (`flex justify-end`), `DirectoryTable`/`EmailTable` headers planos resizables, modals `Grabar` blur con `Rol`/`Área` persistidos; `GeneralesSitiosView` modal `Grabar`, `Directorio` disabled, filtros `Todos+7 modulos` con resizable, consulta `DetailRow` blur.

### 4.2 Backend `intranet-backend`
- **Auth:** `POST /api/auth/login` → BCrypt `PasswordConfig.java:8`, `JwtService HS512 480min`, `SecurityConfig` correcto, `GET /api/me` autenticado.
- **Usuarios:** `genusuario` + `AdminSeeder` `admin/Admin2026**` `99999999`, `UsuarioDtos`, `UserService` con `MAX(oid)+1` fallback, phone preservado frontend.
- **Directorio:** `genarea/genpiso` V11 con Betania/Sistemas, `V12 gendir_ext_tipo` + `V13 gendircorcargo` con `ExtensionDirectorio.tipo`/`CorreoDirectorio.cargo` persistidos (`DirectoryDtos` + `DirectoryService.normalizeTipo`), `DirectoryService` `isSupport` handling, `V10` limpia default.
- **Sitios:** `gensitredireccion` + `genmodulo` 7 módulos, `SiteService` `toggleActive` fix, `Directorio` inhabilitado frontend.

### 4.3 Docker
- `docker-compose.dev.yml` (`8080:8080`, `3000:80`, `SPRING_PROFILES_ACTIVE=dev`, `.env_pruebas`, healthchecks) y `docker-compose.prod.yml` y `docker-compose.yml` unificado validados `docker compose config`. Images `node:20-alpine` builder + `nginx:alpine`, `maven:3.9-eclipse-temurin-21-alpine` + `eclipse-temurin:21-jre-alpine`. Volúmenes `backend-uploads/logs`.

### 4.4 Datos de Prueba Actuales (intraicvc_pruebas)
- **Usuarios (4):** admin, yeferson.sanchez, prueba.comunicaciones, administrativo.prueba (password admin `Admin2026**`, resto `identificacion` tras reset o `Test`).
- **Sitios (6):** Almera (Gestion Institucional), BUK x2 (Area Administrativa), DGH/Enterprise (Area Asistencial), GLPI (Soporte) + Betania test si se crea.
- **Extensiones (2):** Asistente de Ingenieria `342 Betania Sistemas soporte:true` (en Soporte), Profesional de Facturación `434 Piso3 Administrativa` (en Directorio).
- **Correos (1):** Yeferson `asistente.ingenieria@icvc.co Sistemas/Betania` (con isSupport handling).

---

## 5. Verificaciones

```bash
npm run build # vite 2549 modules 531-535kB OK
./mvnw test # 15 tests 0 failures 1 skipped
./mvnw clean package -DskipTests # BUILD SUCCESS
docker compose -f docker-compose.dev.yml build # frontend/backend Built
docker compose -f docker-compose.dev.yml up -d # backend Healthy, frontend Up
curl http://localhost:8080/actuator/health # UP
curl -H "Authorization: Bearer $TOKEN" http://localhost:8080/api/directory/extensions # Betania/Sistemas OK
curl http://localhost:3000/api/sites # proxy 200
```

---

## 6. Cómo Ejecutar

```bash
# Dev
docker compose -f docker-compose.dev.yml up --build
# Prod
docker compose -f docker-compose.prod.yml up --build
# Unificado
docker compose up --build
# Frontend dev
cd Corporate-Intranet-Portal && npm install --legacy-peer-deps && npm run dev # :5173 proxy /api → 8080
# Backend dev
cd intranet-backend && ./mvnw spring-boot:run # -Dspring.profiles.active=dev
```

Env: `intranet-backend/.env` / `.env_pruebas` (`POSTGRES_HOST=192.168.3.121`, `JWT_SECRET`, `ADMIN_*`).

---

## 7. Fases 22/09 — Correcciones Directorio (Betania / Filtros / Área / Cargo)

### Fase 1 (UI sin DB, 22/09 tarde)
- **Filtros tablas admin:** `GeneralesDirectorioView.tsx:302,372` `DirectoryTable`/`EmailTable` filtros movidos fuera de `<Th>` (`flex gap-3 mb-4 justify-end` mismo margen que título, `Piso: Todos`/`Área: Todos`/`Cargo: Todos` `border-2 rounded-lg min-w-[160px]`) — fix diseño roto `table-fixed`+`colgroup`+handle.
- **Normalización:** `directory.ts:46` `normalizeName=trim().toLowerCase()` para `area`/`piso` en `buildExtensionBody`/`buildCorreoBody` + `console.warn` fallback, corrige `Betania`/`betania`/` Betania `.
- **Cargo preservado:** `SystemContext.tsx:501` `addInstitutionEmail/updateInstitutionEmail` ahora mergea `position/area/floor/isSupport` (antes solo `isSupport`).
- **Público Betania:** `DirectoryModule.tsx:82` `handleAdd` ahora envía `area:newArea` (antes `undefined`→`Administrativa`), `vite build 535.68kB` OK.

### Fase 2 (Persistencia DB, 22/09)
- **Migraciones:** `V12__add_tipo_to_extension.sql:1` añade `gendircextenciones.gendirexttipo varchar(20) default 'administrativo' check (asistencial/administrativo)` + índice; `V13__add_cargo_to_correo.sql:1` añade `gendircorreo.gendircorcargo varchar(255)` + backfill `genarea` + índice (idempotentes).
- **Backend:** `ExtensionDirectorio.java:47` `@Column gendirexttipo`, `CorreoDirectorio.java:47` `gendircorcargo`; `DirectoryDtos.java:17,57` `ExtensionRequest/Response` con `tipo`, `CorreoRequest/Response` con `cargo`; `DirectoryService.java:37,72` persiste `tipo`/`cargo` con `normalizeTipo()`.
- **Frontend persistencia:** `mappers.ts:8,31,43` `Backend*Response tipo?/cargo?` + `mapExtensionToFE` lee `dto.tipo`, `mapCorreoToFE` lee `dto.cargo ?? areaNombre`; `directory.ts:66,93` envía `tipo` y `cargo`.
- **Verificación:** `mvn test 15/0`, `mvn package BUILD SUCCESS`, `docker compose build` OK, `GET /api/directory/extensions` ahora retorna `tipo`, `GET /api/directory/emails` retorna `cargo` independiente de `area`.

### Fase 3 (Separación Área pública, 22/09)
- **DirectoryModule público:** Añadido estado `newArea` (`DirectoryModule.tsx:47`), `handleAdd` valida `newArea.trim()` y envía `area:newArea`; modal `Cargar Nuevo Directorio` ahora tiene 3 campos independientes `Nombre de la extensión` + `Área *` + `Número de Extensión` + `Piso(s)` (`DirectoryModule.tsx:416-438`), edit modal también `Nombre` + `Área` (`DirectoryModule.tsx:506-515`); cards ahora muestran `area` chip `bg-gray-50` + `floor` (`DirectoryModule.tsx:225,250`) — alinea con `GeneralesDirectorioView.tsx:189` admin.

### Fase 4 (Verificación E2E, 22/09)
- `npm run build 2549 modules 537.04kB`, `mvn test 15/0 1 skipped`, `mvn package`, `docker compose -f docker-compose.dev.yml build` (backend/frontend Built, `vite 6.3.5`). `docker compose config` validado `healthcheck`/`volumes`/`networks`.
- Flujo manual: Crear extensión `Rol=Asistencial Área=Sistemas Piso=Betania` → aparece en columna Asistencial, filtro `Betania` la lista, reload mantiene `tipo/área` (V12); Crear correo `Área=Sistemas Cargo=Coordinador` → `Área` y `Cargo` filtros independientes, `cargo` no duplica `area` tras reload (V13).

## 8. Pendientes / Notas

- `genusuario` no persiste `phone` en BD; se preserva solo en `localStorage` frontend (`AuthContext` merge). Para persistencia real, añadir columna `genusutel` + migración.
- `Principal` piso legado permanece en BD pero no se muestra en UI (filtrado a Betania).
- `RedirectModal.tsx` conservado sin uso; puede eliminarse.
- **Requiere despliegue:** `V12/V13` se aplican al iniciar contenedor (`flyway baseline-on-migrate`); ejecutar `docker compose -f docker-compose.dev.yml up --build -d` y validar `curl /actuator/health` + `GET /directory/{extensions,emails,floors,areas}`.

