spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

  application:
    name: intranet-backend

  config:
    import: optional:file:./.env[.properties], optional:file:./.env_pruebas[.properties]
